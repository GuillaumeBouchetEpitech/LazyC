
#include <stdio.h>

#include "./math/Vec3.lc"
// import { Vec3 } from "./math/Vec3.lc"

export void test_about_vec3()
{

  // Vec3 posA = Vec3::create(1,1,1);
  // printf(" -> posA: %.3f/%.3f/%.3f\n", posA.x, posA.y, posA.z);
  // {
  //   Vec3 posB = Vec3::create(2,2,2);
  //   printf(" -> posB: %.3f/%.3f/%.3f\n", posB.x, posB.y, posB.z);
  //   {
  //     Vec3 posC = Vec3::create(3,3,3);
  //     printf(" -> posC: %.3f/%.3f/%.3f\n", posC.x, posC.y, posC.z);
  //     // Vec3 sumA = posA + posB;
  //     // Vec3 sumB = posA / (posB - posC);
  //     // sumB += (posA + posB) * posC;
  //     // printf(" -> sumB: %.3f/%.3f/%.3f\n", sumB.x, sumB.y, sumB.z);
  //     // return posA / (posB + posC);
  //   }
  // }
}


